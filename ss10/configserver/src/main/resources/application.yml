spring:
    application:
        name: configserver
    kafka:
      bootstrap-servers: localhost:9092
      topic:
        stock-events: medicine-stock-events-v2
    cloud:
      config:
        server:
          git:
            uri: file:///C:/Users/admin/Desktop/md4/md4_ss9/pharmacy-config-repo
            default-label: master

server:
  port: 8888